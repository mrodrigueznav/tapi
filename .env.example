# Server
PORT=
NODE_ENV=

# Database (Supabase Postgres or any PostgreSQL)
# Reemplaza con tu connection string; nunca subas la contraseña real al repo.
DATABASE_URL=postgresql://USER:PASSWORD@HOST:5432/DATABASE

# Clerk (SDK: @clerk/express). Dashboard → API Keys.
# https://dashboard.clerk.com
CLERK_PUBLISHABLE_KEY=
CLERK_SECRET_KEY=

# Supabase
# Dashboard → Settings → API. Secret key solo en backend.
SUPABASE_URL=https://YOUR_PROJECT_REF.supabase.co
SUPABASE_SECRET_KEY=
SUPABASE_STORAGE_BUCKET=

# Signed URL TTL for document downloads (seconds)
SIGNED_URL_TTL_SECONDS=300

# CORS. Comma-separated origins; supports wildcards e.g. https://*.lovable.app
# Empty in dev allows *; in production empty means restrict.
CORS_ORIGINS=http://localhost:3000,https://*.lovable.app

# Tests: bypass Clerk JWT and use x-test-clerk-user-id header
TEST_AUTH_BYPASS=false
